import { AppAudioType } from './index.t';

const defaultState: AppAudioType | null = null;

const configs = (state: AppAudioType | null = defaultState, action: any): AppAudioType | null => {
  switch (action.type) {
    default:
      return state
  }
}

export default configs;
